# 数据观察页面问题修复计划

## 问题清单

### 1. 每周热帖热度排行问题
**问题描述**：每周热帖应该使用热度进行排行，但当前实现可能存在问题

**解决方案**：
- 检查每周热帖的数据结构和排序逻辑
- 确保热度计算和排序在前端正确实现
- 参考每日热帖的实现方式

### 2. 贴吧当日活跃度排行代码错误
**问题描述**：当日活跃度排行的代码存在 AI 幻觉错误，实现完全错误

**解决方案**：
- 彻底检查 `updateBarChart` 函数的实现
- 重新设计活跃度计算逻辑
- 确保数据流正确：API 返回 → 数据处理 → 图表渲染
- 移除任何 AI 生成的错误代码

### 3. iOS 畅销榜布局问题
**问题描述**：当前 iOS 排行是卡片式布局（一个卡片一个游戏），希望改为一排一个游戏的列表布局

**解决方案**：
- 修改 `.app-ranking-cards` 的 CSS 布局
- 从 `grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))` 改为单列布局
- 优化每个游戏项的展示方式

## 实施步骤

### 第一步：修复每周热帖热度排行
1. 检查 `loadWeeklyHotPosts` 函数的数据加载
2. 检查每周热帖的渲染模板
3. 确保热度值正确显示和排序

### 第二步：重写贴吧当日活跃度排行
1. 删除当前错误的 `updateBarChart` 实现
2. 重新实现活跃度计算逻辑：
   - 热度 = 3 * 评论数 + 点赞数
   - 按热度排序
   - 取前 10 名
3. 确保图表正确渲染

### 第三步：修改 iOS 排行布局
1. 修改 `.app-ranking-cards` 样式为单列布局
2. 调整 `.app-ranking-card` 样式
3. 确保响应式设计

### 第四步：测试验证
1. 验证每周热帖的热度排序
2. 验证当日活跃度排行的计算
3. 验证 iOS 排行的布局显示

## 验收标准

1. ✅ 每周热帖按热度正确排序
2. ✅ 当日活跃度排行计算逻辑正确
3. ✅ iOS 排行显示为一排一个游戏的列表
4. ✅ 所有功能正常工作，无控制台错误
