# WutheringWavesDPS Nginx 配置
# 适用于 RHEL/CentOS/OpenCloudOS 系统

server {
    listen 80;
    server_name www.arcanamorning.tech;

    # 安全头部
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # 让 Let's Encrypt 验证
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    # 应用代理配置
    client_max_body_size 50M;

    location /WutheringWavesDPS/ {
        proxy_pass http://127.0.0.1:14876;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时配置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# HTTPS 配置（需要先申请 SSL 证书）
# server {
#     listen 443 ssl http2;
#     server_name www.arcanamorning.tech;
# 
#     # SSL 证书配置（需要先申请证书）
#     ssl_certificate /etc/letsencrypt/live/www.arcanamorning.tech/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/www.arcanamorning.tech/privkey.pem;
# 
#     # SSL 安全配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
# 
#     # 安全头部
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
# 
#     # 应用代理配置
#     client_max_body_size 50M;
# 
#     location /WutheringWavesDPS/ {
#         proxy_pass http://127.0.0.1:14876;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         
#         # WebSocket 支持
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         
#         # 超时配置
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
# }
# 
# # 重定向 HTTP 到 HTTPS
# server {
#     listen 80;
#     server_name www.arcanamorning.tech;
#     return 301 https://$server_name$request_uri;
# }
